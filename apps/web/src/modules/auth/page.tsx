import { LoginForm } from './login-form';

export default function AuthPage() {
  return (
    <main style={{ padding: '4rem', display: 'grid', placeItems: 'center' }}>
      <LoginForm />
    </main>
  );
}
