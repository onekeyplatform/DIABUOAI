# Users Module

## Purpose

Manages user identity, profile state, and tenant membership within the multi-tenant platform.

## API

- `GET /users`
- `GET /users/:id`
- `POST /users`
- `PUT /users/:id`

## Notes

This module is directly dependent on the Organization and Authentication modules and forms the human identity layer for the platform.
